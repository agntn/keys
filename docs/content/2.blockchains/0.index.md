---
title: Blockchains
icon: i-lucide-boxes
navigation:
  title: Overview
description: Fourteen drivers and one class shape. Pick a chain to see its address rules and the traps.
---

Every driver below answers the same calls: `generateWallet`, `getAddress`, `validateAddress`, `signMessage`, `deriveHDWallet`. What changes is the hashing, the encoding, and which arguments actually matter. The pages are short on purpose, the interesting part of each one is the gotcha list.

:::card-group
  ::card
  ---
  icon: i-token-btc
  to: /blockchains/bitcoin
  title: Bitcoin
  ---
  secp256k1. Five address formats, testnet, and a taproot that does the real BIP341 tweak.
  ::

  ::card
  ---
  icon: i-token-bch
  to: /blockchains/bitcoincash
  title: Bitcoin Cash
  ---
  secp256k1. Bitcoin's key and hash written as CashAddr, `bitcoincash:q...`, and nothing else.
  ::

  ::card
  ---
  icon: i-token-btg
  to: /blockchains/bitcoingold
  title: Bitcoin Gold
  ---
  secp256k1. Bitcoin's formats behind `G`, `A` and `btg1q`, and no taproot, because the chain never turned it on.
  ::

  ::card
  ---
  icon: i-token-bsv
  to: /blockchains/bitcoinsv
  title: Bitcoin SV
  ---
  secp256k1. Bitcoin's `1` address, byte for byte, and no P2SH since Genesis.
  ::

  ::card
  ---
  icon: i-token-ltc
  to: /blockchains/litecoin
  title: Litecoin
  ---
  secp256k1. Bitcoin's five formats behind `L`, `M` and `ltc1`, plus its own message preamble.
  ::

  ::card
  ---
  icon: i-token-dcr
  to: /blockchains/decred
  title: Decred
  ---
  secp256k1. BLAKE-256 in every hash, addresses that start with `Ds`, and no HD walk, on purpose.
  ::

  ::card
  ---
  icon: i-token-eth
  to: /blockchains/ethereum
  title: Ethereum
  ---
  secp256k1. EIP-55 checksummed hex and the personal_sign preamble.
  ::

  ::card
  ---
  icon: i-token-base
  to: /blockchains/base
  title: Base
  ---
  Same driver as Ethereum under a different name. Same key, same address.
  ::

  ::card
  ---
  icon: i-token-trx
  to: /blockchains/tron
  title: TRON
  ---
  secp256k1. Ethereum's 20 bytes wrapped in base58check, so it starts with T.
  ::

  ::card
  ---
  icon: i-token-sol
  to: /blockchains/solana
  title: Solana
  ---
  ed25519. The address is the public key in base58, no hashing at all.
  ::

  ::card
  ---
  icon: i-token-xlm
  to: /blockchains/stellar
  title: Stellar
  ---
  ed25519. The public key as a `G` StrKey with a CRC16 checksum, and SEP-53 message signing.
  ::

  ::card
  ---
  icon: i-token-apt
  to: /blockchains/aptos
  title: Aptos
  ---
  ed25519. SHA3-256 over the key and a scheme byte, 0x hex out.
  ::

  ::card
  ---
  icon: i-token-sui
  to: /blockchains/sui
  title: Sui
  ---
  ed25519 or secp256k1 on one chain. Blake2b over a flag byte and the key.
  ::

  ::card
  ---
  icon: i-token-ada
  to: /blockchains/cardano
  title: Cardano
  ---
  ed25519. bech32 base, enterprise, and stake addresses. No mnemonic derivation, on purpose.
  ::
:::
